import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF2563EB);
  static const Color secondary = Color(0xFF10B981); // Untuk sukses/aksen
  static const Color background = Color(0xFFF3F4F6); // Untuk latar
  static const Color surface = Colors.white; // Untuk card/container
  static const Color textPrimary = Color(0xFF1F2937); // Untuk teks utama
  static const Color textSecondary = Color(0xFF6B7280); // Untuk teks pendukung
}
